{
  "name": "mihiko-landing",
  "version": "1.0.0",
  "description": "Михико — лендинг с Decap CMS и SEO-оптимизацией",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve public"
  },
  "engines": {
    "node": ">=16"
  }
}
